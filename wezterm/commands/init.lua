local toggle_background = require("commands.toggle-background")
local toggle_transparent = require("commands.toggle-transparent")

local commands = {
	toggle_background,
	toggle_transparent,
}

return commands
